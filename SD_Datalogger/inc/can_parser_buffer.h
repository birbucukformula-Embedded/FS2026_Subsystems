#ifndef CAN_PARSER_BUFFER_H
#define CAN_PARSER_BUFFER_H

#include <stdint.h>

// TODO: CAN simülasyonu, CSV ve Buffer fonksiyon prototipleri buraya eklenecek.
/*
typedef struct {
    uint32_t timestamp_ms;
    float speed_kmh;
    float engine_temp_c;
    uint8_t error_code;
} CAN_DataFrame_t;

void CAN_SimulateData(CAN_DataFrame_t* frame);
*/

#endif /* CAN_PARSER_BUFFER_H */
